import 'package:flutter/material.dart';

class SmallTetx extends StatelessWidget {
  final String text;
  
  const SmallTetx({super.key, required this.text});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}